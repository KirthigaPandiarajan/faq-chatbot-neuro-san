# Windows-compatible requirements (pre-built wheels only)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
aiofiles==23.2.1
pyyaml>=6.0
requests==2.31.0

# Note: neuro-san>=0.1.0 requires C++ compilation on Windows
# For local development without Visual C++ Build Tools, comment it out
# For production, use Docker instead
